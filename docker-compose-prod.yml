services:
  api:
    image: beatrizruiz981/api-featurehub:1.1 
    restart: always
    depends_on:
      - db
    env_file: 
    - ".env"
    # ports:
    #   - "8000:8000" # <IP:port_local>:<port_container>
  db:
    image: postgres:16 # Añadir version para evitar problemas de compatibilidad
    restart: always
    environment:
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_USER: ${DB_USERNAME}
      POSTGRES_DB: ${DB_NAME}
    volumes:
      - postgres-db:/var/lib/postgresql/data
  frontend:
    image: beatrizruiz981/nginx-front-featurehub:1.1
    ports:
      - "80:80"
    depends_on:
      - api

volumes:
  postgres-db: